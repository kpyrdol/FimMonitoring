@model CustomerViewModel

@{
    ViewBag.Title = "Customer Details";
}

<ol class="breadcrumb">
    <li><a href="@Url.Action("Index", "Import")"><i class="fa fa-dashboard"></i> Home</a></li>
    <li><a href="@Url.Action("Index", "Import")">@Model.SystemName</a></li>
    <li class=""><a href="@Url.Action("Index", "Import")">Customers</a></li>
    <li class="active">Customer details</li>
</ol>

<div class="box box-primary">
    <div class="box-header with-border">
        <h3 class="box-title">Customer: <strong>@Model.CustomerName</strong></h3>
    </div>
    <div class="box-body">
        @Html.Partial("_Carriers", Model.Carriers)
    </div>
</div>